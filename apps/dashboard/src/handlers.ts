/** Job handlers registered in the worker — one source for all UI dropdowns. */
export const HANDLERS = [
  "send-email",
  "resize-image",
  "send-digest",
  "refund",
  "generate-report",
] as const;
